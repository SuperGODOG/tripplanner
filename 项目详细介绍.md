# TripPlanner 项目详细介绍

> 面向 Agent 方向面试的全景项目描述，覆盖主业务流程、副业务产出、系统边界处理、未来规划。

---

## 一、项目定位

TripPlanner 是一个多智能体旅行规划系统。用户输入目的地、出发地、天数、偏好标签，系统自动编排景点、酒店、天气、交通，生成结构化多日行程，并具备跨会话学习用户偏好的能力。

**一句话：** 输入城市和天数，四个 Agent 自动跑完景点搜索、酒店推荐、天气查询、行程规划，最终返回一份每天去哪、住哪、花多少钱的完整计划。

**与传统旅行 App 的本质区别：** 携程告诉你有什么，我们帮你排顺序。携程是货架，我们是规划师。

---

## 二、主业务流程

用户打开前端页面，填写目的地城市（如"成都"）、出发地（"上海"）、天数（3 天）、偏好标签（历史文化、美食），点击提交。

### API 层预处理（不占 LLM 资源）

后端 FastAPI 收到 POST 请求，在进入 Agent 编排之前，先在 API 层做三件事：

1. **日期计算**：纯 Python 本地算日期列表，不调 LLM
2. **天气查询**：直接调高德 `maps_weather` API，拿到目的地一周天气预报；失败就写通用穿衣建议
3. **城际交通**：调高德 `maps_geo` 拿到两城坐标 → 算直线距离 → 判断高铁还是飞机 → 查静态字典估算时间和费用

三项结果打包进 State，交给 LangGraph。

### LangGraph 四节点流水线

图结构：`attraction → hotel → memory → planner → [conditional] → END`

**节点一：景点搜索**

- Agent 调用高德 `maps_around`，以城市中心为圆心、20 公里半径搜索景点
- Wrapper 内部并发调 `maps_geo` 为每个景点补经纬度（ThreadPool，5 线程）
- Node 本地计算所有景点的地理质心（`avg(lng), avg(lat)`），写入 State 传给下游

**节点二：酒店推荐**

- 读取景点质心坐标，调 `maps_around(center, radius=5km)` 就近搜酒店
- 如果无质心（景点搜索失败），退化为全城 `text_search`
- 支持 `center_override`：当 Planner 校验发现离群景点并重算中心后，酒店 Node 用新中心重新搜索

**节点三：记忆加载**

- 纯本地 IO，不调 LLM
- 读取用户历史画像文件（JSON/SQLite），加载到 State 的 `user_profile` 字段
- 用户出行≥5 次时画像生效，否则为空

**节点四：行程规划**

- 把景点、酒店、天气、画像、日期、城际交通全部注入 LLM prompt
- LLM 生成结构化多日行程 JSON：每天包含景点列表、酒店、餐饮预算、交通建议
- Planner 是无工具纯推理 Agent——不需要调 API，只需要整合上游数据

### 校验与回环

Planner 生成后不直接返回。先过本地校验函数 `_validate_and_refine()`：

| 检测类型 | 内容 | 动作 |
|---------|------|------|
| 硬伤 | 每天景点<2、预算超偏好 30%、缺必填字段 | `retry_planner` 自回环，≤3 次 |
| 软伤 | 酒店到最远景点>10km、暴雨天安排户外 | 写 `error_log`，不阻断 |
| 离群检测 | 景点距离 > `mean + 1.5σ` 或 > 80km | 排除离群点 → 重算质心 → `retry_hotel`，≤2 次 |

重试耗尽后强制输出，带上所有 warning。

### 前端展示

响应通过 POST 返回 JSON，前端渲染：

- 每日行程卡片（景点 / 酒店 / 餐饮）
- 天气预报条
- 预算饼图
- 城际交通信息
- 降级面板（`error_log` 透明展示）

进度条用定时器模拟动画（4 步 × 4 秒），不等 SSE。

---

## 三、副业务：让系统"越用越懂你"

### 记忆系统

**五因子权重公式：** `final_weight = domain × decay × interaction × frequency_boost × outlier_penalty`

- **domain_weight**：领域先验（如"住宿偏好"天然比"景点偏好"更稳定）
- **time_decay**：指数衰减，最近行为权重高
- **interaction**：操作类型修正（"点击"vs"确认预订"权重不同）
- **frequency_boost**：高频行为提权（连续 5 次经济型 → 高置信度）
- **outlier_penalty**：异常行为惩罚（5 次经济型后 1 次豪华 → 打 0.3 折）

**双轨异常检测：** 数值型数据（如酒店价格）走 IQR 法（Q3 + 2×IQR 外为异常），分类型数据（如饮食口味）走频率比法（低于众数频次 30% 即 penalty=0.3）。两轨独立，互不干扰。

**渐进构建：** `trip_count < 5` 时画像不生效，前端显示"正在构建画像 3/5"。避免小样本误判——一次偶然行为不应决定用户画像。

### Wrapper 模式：Token 成本优化

高德 MCP 有 16 个工具，Agent 不应关心调用细节。Wrapper 把三层封装为一个 Tool：

```
Agent 视角：调 amap_search(city, type) → 拿到干净文本
内部实际：MCP 调用 → 坐标增强 → Format(800字→300字) → Validate
```

- Format 和 Validate 是纯 Python 确定性处理，不消耗 LLM token
- 坐标增强用 ThreadPool 并发 `maps_geo`（10 个 POI 从串行 5s → 并行 1s）
- 每次调用节省约 60% token 消耗

### 容错设计

| 层级 | 机制 | 示例 |
|------|------|------|
| 数据源级（L1） | 自动切换备用方案 | 天气 API 挂了 → 通用穿衣建议 |
| Agent 级（L2） | Error-as-Observation | 景点搜索失败 → 写 `error_log` → 下游继续 |
| 图级（L3） | Conditional Edge 重试上限 | Planner 硬伤自回环 ≤3 次、酒店离群重算 ≤2 次 |
| 全链路兜底 | FallbackTool | 三个数据源全挂 → 输出"请到达后咨询旅游中心" |

加上 MCP 10s 超时、LLM 指数退避重试（1s→2s→4s）、SqliteSaver 断点续传。

---

## 四、系统边界：什么放哪、为什么

**原则：确定性工程计算放 API 层，需要语义推理的才进图内。**

| 放 API 层（预处理） | 放图内（Agent 编排） |
|-------------------|---------------------|
| 日期计算 | 景点搜索 |
| 天气查询 | 酒店推荐 |
| 城际交通距离/时间/费用 | 行程规划 |
| — | 记忆加载（IO 但独立节点） |

**为什么天气不在图里？** 天气是纯查询，不依赖 Agent 数据、不需要 LLM、不需要 Checkpoint。和城际交通一样，属于"请求预处理"——失败时 API 层直接写降级，不消耗 ReAct 轮次。

**为什么记忆是独立节点？** 当前是单用户 JSON 文件 IO，后续多用户场景需要独立节点做向量检索 + 缓存调度。独立节点保留架构灵活性。

---

## 五、技术栈

| 层级 | 技术 | 作用 |
|------|------|------|
| Agent 框架 | HelloAgents (SimpleAgent + ReAct) | 单个 Agent 的推理循环 |
| 图编排 | LangGraph (StateGraph + ConditionalEdge) | 多 Agent 流水线 + 校验回环 |
| 工具协议 | MCP (Model Context Protocol) | 高德地图 16 个工具的标准化接入 |
| LLM | DeepSeek Chat | 语义理解和生成 |
| 后端 | FastAPI + Pydantic | REST API + Schema 校验 |
| 前端 | Vue 3 + Vite | 单页应用，POST 轮询 |
| 存储 | JSON 文件 + SQLite (Checkpointer) | 记忆持久化 + 断点续传 |
| 容错 | SqliteSaver + 指数退避 + 10s 超时 | 进程重启恢复 + API 故障降级 |

---

## 六、关键代码量

| 模块 | 文件 | 行数 | 职责 |
|------|------|------|------|
| 图编排 | graph/builder.py, state.py, nodes.py | ~500 | 4 Node 定义 + conditional edge |
| Agent 定义 | agents/trip_planner_agent.py | ~250 | 4 个 SimpleAgent + prompt 模板 |
| Wrapper | tools/amap_wrapper.py | ~200 | 三层封装 + geo 增强 + 超时 |
| 记忆 | memory/manager.py, classifier.py | ~200 | 五因子权重 + 双轨异常检测 + 去重 |
| API | api/trip.py, models/schemas.py | ~300 | 端点 + Pydantic Schema + 预处理 |
| 前端 | frontend/src/App.vue | ~500 | 单文件 Vue 组件 |
| 容错 | 分布在各文件 | ~200 | LLM 退避/MCP 超时/Checkpoint/去重/取消传播 |
| **总计** | | **~2150** | — |

---

## 七、未来规划

### 功能补全
- Agent 反馈环（Planner ↔ Hotel 迭代调优），已预留 Conditional Edge 接口
- 多用户协同规划
- RAG 知识库集成（游记、攻略作为参考上下文）

### 性能优化
- LLM 调用并行化（景点/酒店可并行搜索，不依赖对方结果）
- Redis 缓存热点城市 POI 和天气

### 算法升级
- DBSCAN 聚类替代标准差选址法
- 用户画像从规则匹配升级为 embedding 向量语义检索

### 产品化
- 实时行程动态调整（暴雨自动改室内景点）
- A/B 评测体系：L1 自动化 + L2 约束检查 + L3 LLM-as-Judge + L4 人工标注
- 消息队列异步化（非关键路径如记忆更新放入队列）

---

> 基于 PROJECT.md / ARCHITECTURE.md / 面试问答.md 综合撰写，最后更新 2026-07-25
