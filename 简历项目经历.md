# TripPlanner 项目经历

---

## 版本 1：Agent实习方向

**基于 LangGraph 的多 Agent 旅行规划系统** 2025.7-2025.7

独立设计并落地 4-Agent 协同规划系统，解决多源数据整合、跨会话记忆与行程空间合理性校验问题。

- **Agent 编排**：基于 LangGraph StateGraph 搭建 4-Node 流水线（景点→酒店→记忆→规划），设计 Conditional Edge 三路路由——硬伤回退重新生成（≤3 次）、离群触发酒店就近重搜（≤2 次）、校验通过直接输出，error_log 通过 Annotated[list, add] 全链路累积不覆盖。

- **工具封装**：将高德 MCP 16 个工具封装为单一 Wrapper 接口，内部走三层流水线——MCP 调用获取原始 POI → ThreadPool 并发 maps_geo 补经纬度（10 个 POI 从串行 5s 降至并行 1s）→ Format 层纯 Python 将 800 字原始 JSON 压缩至 300 字传 LLM，单次工具调用省 60% token。

- **记忆系统**：设计五因子权重公式（domain × decay × interaction × frequency_boost × outlier_penalty）计算偏好置信度，配合双轨异常检测——数值型走 IQR 箱线图（Q3+1.5IQR 外标记离群）、分类型走频率比（低于众数 30% 即 penalty=0.3）。画像渐进生效（trip_count≥5），5 次经济型之后的单次豪华消费不被污染。

- **空间校验**：实现自适应离群检测——计算景点群距离分布的均值与标准差，以 mean+1.5σ 为阈值标记离群点，叠加 80km 硬上限兜底极端场景（如重庆 8 万 km² 市域跨度）。排除离群点后重算质心触发酒店就近重搜。

- **容错保障**：落地 6 项生产级能力——SqliteSaver Checkpoint 断点续传（进程重启从最后成功 Node 恢复）、LLM 指数退避重试（1s→2s→4s + jitter，应对 429/5xx）、MCP 调用 10s 超时、SSE 取消传播、记忆写入去重、thread_id 请求隔离。总计 200 行新增，零删除已有代码。

- **并发优化**：识别三处独立子任务串行→并行——API 预处理层 `_compute_intercity ∥ _fetch_weather`（预处理耗时从 `t1+t2` 变为 `max(t1,t2)`，长途路线省 1-3s）、城际交通内 origin/destination 双 `maps_geo` 并发（省一次 MCP round-trip ~200-500ms）、LangGraph 拓扑改 fan-out（`START→[attraction, memory]` 并行入口，planner 双入边 join，纯本地读的 memory 与 LLM+MCP 长任务不阻塞）。全部用 ThreadPoolExecutor + `error_log: Annotated[list, add]` reducer 自动合并，**无需应用层 Lock**——通过读 `hello_agents.MCPTool.run()` 源码确认每次调用内部新建独立 event loop + MCPClient 连接，无共享 mutable state。改动 30 行，零架构侵入。

- **项目成果**：端到端响应 30-60s 输出完整多日行程 JSON，三层降级（API 备用→Agent 降级标注→本地模板兜底）保障外部数据源故障不死机。2150 行 Python/Vue 代码，API 层与编排层完全解耦。记忆系统在 5 次以上使用后有效区分偶然行为与稳定偏好。离群检测与酒店回环机制已在实际测试中通过重庆、北京等极端城市验证。

---

## 版本 2：央国企科技岗

**基于分层架构的旅行规划系统** 2025.7-2025.7

设计并落地五层架构的行程规划系统，重点解决外部数据源不可靠时的系统稳定性与输出可解释性问题。

- **分层设计**：五层架构（LLM 调用→Agent 循环→工具封装→图编排→业务 API），每层独立职责与错误处理，层间通过 Pydantic 模型约定接口。确定性计算（日期、天气、城际交通）下沉 API 层预处理，语义推理进图编排层，职责分离清晰。

- **双层容错**：图层面通过 Conditional Edge 管理节点间跳转——行程生成不通过自动重试、景点分布异常触发酒店重搜，均设次数上限防止死循环。Agent 层面异常不抛错，改为写 error_log 继续执行，下游节点感知到降级状态后调整策略。

- **三层降级**：L1 数据源级自动切换备用方案（天气 API 故障返回通用穿衣建议、城际 API 故障走静态距离表），L2 Agent 级返回降级标注并继续流转，L3 全链路 FallbackTool 本地模板兜底。所有降级信息累积至前端面板透明展示。

- **空间分布校验**：景点搜索结果经自适应离群检测过滤——标准差法（mean+1.5σ）排除远郊景点后重算质心，叠加 80km 硬上限应对极端城市尺度。酒店以最终质心为基准就近搜索，确保行程空间合理性。

- **生产保障**：SqliteSaver Checkpoint 持久化（进程重启断点续传）、LLM 指数退避重试（应对 API 超时与限流）、MCP 超时保护（10s）、SSE 取消传播（前端断开释放后台资源）、记忆写入去重、请求级状态隔离——六项改动均为增量补充，不修改已有逻辑。

- **性能优化**：三处独立任务串行改并行——API 预处理层天气查询与城际交通查询并发（ThreadPoolExecutor 双 worker）、城际交通内起点/终点地理编码双查并发、图编排层拓扑改 fan-out 让记忆加载与景点搜索并行执行。改造前先做底层线程模型分析——通过读 MCPTool 源码确认每次调用内部新建独立事件循环与客户端连接，实例上无共享可变状态，多线程并发调用安全，无需应用层加锁。改动 30 行，端到端跑通验证（graph.invoke 76.6s，final_plan 完整），零架构侵入。

- **项目成果**：系统在外部 API 全故障时仍可输出包含降级说明的完整行程 JSON。渐进画像构建（trips<5 不生效）避免小样本误判。代码总量 2150 行，前后端分离，API 层与编排引擎可独立替换。

---

> 最后更新：2026-07-26
