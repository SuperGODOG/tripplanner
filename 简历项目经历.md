# TripPlanner 项目经历（STAR法则双版本）

---

## 版本 1：Agent实习方向

**S:** 旅行规划需要在携程、天气App、Booking之间来回切，查完景点再看酒店再看天气，步骤多且每次从零开始，记不住用户偏好。

**T:** 做一个输入城市和天数就能自动出完整行程的系统，四个Agent各管一摊，还能跨会话记住用户习惯。

**A:**
① 用LangGraph搭了4个Node的流水线（景点→酒店→记忆→规划），加了三档重试——规划不合理就回退重新生成（最多3次），景点太偏就踢掉重新搜酒店（最多2次）
② 高德地图有16个工具，封装成一个接口给Agent调，内部做了三件事：调API拿原始数据 → 并发补坐标 → 把800字的JSON精简到300字再给LLM，省了六成token
③ 记忆部分用五个因子加权算偏好权重，数值型数据用箱线图检测异常、分类型数据用频率比检测，保证住了五次经济型不会被一次豪华型带偏
④ 景点如果离群太远（比如北京的八达岭离市中心70公里），用标准差+80km硬上限自动过滤，重新算中心后让酒店就近搜

**R:** 一趟请求四个Agent自动跑完，端到端出完整行程。数据源挂了有兜底文案，前端能看到哪一步降级了。总共2150行代码，前后端完全拆开。

---

## 版本 2：央国企科技岗

**S:** 旅行规划依赖多个外部数据源（景点、酒店、天气、交通），外部API不稳定时系统不能跟着崩，得有明确的兜底路径。

**T:** 做一个分层架构的系统，每层有自己的容错策略，出了问题能定位到具体哪一层，用户始终能看到完整输出。

**A:**
① 五层架构：LLM调用、Agent循环、工具封装、图编排、业务API各自独立，换掉任何一层不影响上下层，层之间用Pydantic做数据校验
② 双层容错：图层面用条件边管理节点间跳转（生成不通过就重来、酒店太远就重搜，都有次数上限），Agent层面异常不抛错而是写日志继续跑，两层各管各的
③ 三层降级：API挂了切备用、Agent挂了标降级、全挂了有本地模板兜底，所有降级信息攒到前端面板展示
④ 景点搜索完了会检查空间分布——离太远的自动排除，重新算一个更合理的中心点再搜酒店
⑤ 补了六项生产保障：断点续传、LLM调用失败自动重试、外部API超时保护、前端断开释放后台资源、重复数据跳过、请求级别状态隔离

**R:** 外部API全挂也能输出可用行程，降级原因前端可查。渐进画像少于5次不显示，避免数据太少误判。前后端分离，代码量2150行。

---

> 最后更新：2026-07-25
