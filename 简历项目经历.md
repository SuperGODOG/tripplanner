# TripPlanner 项目经历（STAR法则双版本）

> 基于项目文档与面试问答综合撰写，面向不同求职场景。

---

## 版本 1：Agent实习方向（大厂/创业公司）

> 适用场景：投递AI Agent/LLM应用开发实习岗，面试官关注架构设计、编排能力与算法创新。

**S:** 传统旅行App需用户手动在携程、天气、Booking等多平台间切换拼凑行程，且无法区分偶然行为与长期偏好——每次搜索从零开始，用户画像易被单次异常行为破坏。

**T:** 独立设计并实现一个具备多Agent协作、跨会话记忆与自主决策能力的旅行规划系统，输入城市与天数即可自动输出完整行程。

**A:**
① 基于LangGraph构建4-Node StateGraph流水线（景点→酒店→记忆→规划），设计Conditional Edge路由实现硬伤自回环重生成（≤3次）与离群检测触发酒店就近重搜（≤2次），通过Annotated[list, add]实现全链路error_log累积
② 设计Wrapper模式——将高德MCP 16个工具封装为单一接口，内部走MCP调用→坐标增强（ThreadPool并发maps_geo补经纬度）→格式化（800字原始JSON压缩至300字）→校验三层流水线，Agent单次工具调用省60% token
③ 实现五因子权重记忆系统（domain×decay×interaction×frequency_boost×outlier_penalty）配合双轨异常检测——数值型走IQR统计分布（如酒店价格Q3+1.5IQR外为离群），分类型走频率比（低于众数30%即penalty=0.3），确保5次经济型之后的单次豪华消费不污染长期画像
④ 设计自适应离群检测算法——标准差法（mean+1.5σ）叠加80km硬上限兜底，自动过滤远郊景点并重算景点群质心→触发酒店就近重搜，避免将八达岭纳入北京市中心一日游
⑤ 落地6项interrupt-resilient容错——SQLite Checkpoint断点续传、LLM指数退避重试、MCP 10s超时、SSE取消传播、记忆去重、thread_id配置——总计200行新增零删除

**R:** 系统实现端到端自动化：输入城市与天数，4 Agent自动完成景点搜索、天气查询、酒店推荐、行程规划。5次使用后画像渐进生效，三层降级（数据源→Agent→全链路FallbackTool）保障故障不死机且降级原因前端透明可追溯。总计2150行Python/Vue代码，API层与编排层完全解耦。

---

## 版本 2：央国企科技岗

> 适用场景：投递央国企/银行/运营商科技岗位，面试官关注系统可靠性、分层设计、工程规范与容错机制。避免"创新""自研"等花哨表述。

**S:** 旅游行程规划涉及多数据源整合（景点、酒店、天气、交通），各数据源可靠性参差不齐，系统需要在不稳定外部依赖下稳定输出可用的规划结果。

**T:** 构建一个分层架构的旅行规划系统，确保各级数据故障时有明确降级路径，用户始终获得完整且可解释的输出。

**A:**
① 采用五层架构设计（LLM调用层→Agent内循环层→框架封装层→图编排层→多智能体编排层），每层独立职责与错误处理策略，层间通过Pydantic模型约定接口，换编排引擎不影响API层
② 建立双层容错机制——图级Conditional Edge管理节点间路由（硬伤重生成、离群回环重算均有上限），Agent级Error-as-Observation管理工具调用内重试决策，两层独立互不耦合
③ 设计三层降级策略——L1数据源级自动切换备用API、L2 Agent级返回降级标注、L3全链路FallbackTool本地模板兜底，所有降级信息通过error_log累积并前端透明展示
④ 实现景点空间分布校验——自适应离群检测（标准差法+80km硬上限）排除远郊景点后重算中心，确保酒店搜索范围落在景点群周边而非城市另一端
⑤ 补齐6项interrupt-resilient生产级保障——SQLite Checkpoint持久化（进程重启断点续传）、LLM指数退避重试（应对API超时/限流）、MCP超时保护（10s兜底）、SSE取消传播（前端断开释放后台资源）、记忆去重（防止重复写入）、thread_id配置（Checkpoint恢复前提）

**R:** 系统在任意数据源故障时保持可用——三层降级确保不崩溃，降级原因从前端面板可追溯。Pydantic自动完成请求参数校验（类型/范围），渐进画像构建（少于5次不出画像）避免小样本误判。代码结构清晰，API层与业务层完全分离，总计2150行代码覆盖全链路。

---

> 基于项目代码实际与面试文档综合撰写。最后更新：2026-07-25
